You are implementing GOAT Yu-Gi-Oh card audits. Do not run effect-audit:run recursively.

Process exactly these 1 card(s), in this order. Do not process any other card:
- #1 83994646 4-Starred Ladybug of Doom (Monster; Effect, Flip)
  Work packet: src/engine/auditWork/0001-83994646.json
  Review spec: src/engine/cardReviews/0001-83994646-4-starred-ladybug-of-doom.md
  Checkpoint: src/engine/auditWork/checkpoints/0001-83994646.json

Fast mode:
- Do not run npm run typecheck, npm run test, npm run build, or npm run effect-progress:report inside this worker.
- Do not add per-card executable tests in fast mode unless a reusable engine primitive needs an inline smoke fixture.
- Still write acceptanceTests in each manifest row as concrete test cases to implement/run during the final sweep.
- For verified effect-bearing cards without an executable test yet, set testFile to "deferred:src/engine/__tests__/engine.test.ts".

Requirements:
1. For each listed card, read the exact local card record from yugioh_cards/cards.json.
2. Check Goat World and GoatFormat ruling sources listed in that card's work packet. Use web search if needed.
3. Complete each review spec with timing, costs, targets, trigger/continuous/replacement behavior, battle/Damage Step behavior, and edge cases.
4. Implement missing shared engine primitives if needed.
5. Implement each card's script, or mark it no-op only if it is truly a no-effect card after review.
6. Record focused acceptance test descriptions; executable tests are deferred to the final sweep.
7. Do not read or edit src/engine/effectProgress.json. Write the listed checkpoint JSON file after each card; the parent runner will merge it.
8. A verified checkpoint must include auditVersion 1, order, passcode, status, reviewedAt, completedAt, localTextHash, implementationSummary, rulingNotes, acceptanceTests, implementationFile, testFile, and notes.
9. Skip command verification in this worker; final validation is deferred.
10. If any listed card cannot be completed, write that card's checkpoint with status engine-needed, blocked-ruling, or failed plus a precise notes value, then stop.
